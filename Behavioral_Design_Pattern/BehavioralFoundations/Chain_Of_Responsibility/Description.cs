namespace Behavioral_Design_Pattern.BehavioralFoundations.ChainOfResponsibility
{
    internal class Description
    {
        // it is statefull
        // the trigger of the action starts with christine to meet
        // with the client and keeps on rolling
        // if an error occurs on anywhere after christine u have to rollback till christine
        //not used these days since microservices and looks like state pattern check.

        //1. Chain of Responsibility
        //Avoid coupling the sender of a request to its receiver by giving more then one object a
        // chance to handle the request.Chain the receiving objects and
        //pass the request along the chain until an object handles it.
    }
}
