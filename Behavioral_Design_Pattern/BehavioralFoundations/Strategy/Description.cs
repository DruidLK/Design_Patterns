namespace Behavioral_Design_Pattern.BehavioralFoundations.Strategy
{
    internal class Description
    {
        //dynamicly changing the method behavior at runtime
        //defines a family of algorithm and encapsulate them

        //let us assume a hero has different types of power he can 
        // be regular fly shoot laser
        // the instantiating of the class will make the hero regular
        // and the changing of the power is done through a method
        // since the constructor is private
    }
}
