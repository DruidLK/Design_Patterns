namespace Behavioral_Design_Pattern.BehavioralFoundations.Visitor
{
    internal class Description
    {
        //11. Visitor Represent an operation to be performed
        //on the elements of an object structure.
        //Visitor lets you define a new operation without
        //changing the classes of the elements on which it operates.

        //steps:
        //define an interface that represent elements
        //defind concrete classes of the elements
        //define an interface that represent the visitor
        //define concrete classes of the visitor
        //loop through each one recursion or other way
    }
}
