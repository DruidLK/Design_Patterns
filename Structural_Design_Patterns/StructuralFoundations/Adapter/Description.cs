namespace Structural_Design_Patterns.StructuralFoundations.Adapter
{
    internal class Description
    {
       //17. Adapter (mapper) Convert the interface of
       //a class into another interface clients
       //expect.Adapter lets classes work
       //together that couldn't otherwise
       //because of incompatibility interfaces.

        //do not change the underlying Code, but build
        //on top of it

        //assume ur using 3rd party api provider
        //instead of re-creating the methods
        //just use adapter that takes
        //the outputs and change the result to
        //the local models
    }
}
