namespace Creationnal_Design_Patterns.CreationnalFoundations.Builder
{
    internal class Description
    {
        //13. Builder Separate the construction of a complex object from its representation so
        //that the same construction processes can create different representations.

        // it solves the situation of increasing constructor
        // parameters and constructirs of a given class by providing
        // a step by step initialization of parameters.
        // after step by step it returns the resulting constructed
        // object at once

        //let us assume we want to build a house, house with garden, with status, with pool,etc...
    }
}
