namespace Creationnal_Design_Patterns.CreationnalFoundations.Factory_Design
{
    internal class Description
    {
        //same principle of Factory method, however to solve the problem of
        //the store concerning only 1 style of pizza we pass the IPizzaFactory to the base
        // to be used by all the subclasses while also per default the class can instantiate the 
        // default one.
    }
}
